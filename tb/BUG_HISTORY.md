# BUG_HISTORY.md — frame_rcv_ip bug ledger

This ledger records real RTL and DV-harness bugs found during verification.
Each fixed bug must point at the testcase or regression that exposed it and the git commit that carried the fix.

| bug_id | date | scope | ip_or_tb | first_seen_case | first_seen_runtime_s | coverage_or_signoff_context | symptom | root_cause | fix_summary | fix_commit | status |
| --- | --- | --- | --- | --- | ---: | --- | --- | --- | --- | --- | --- |
| FRCV-2026-04-16-001 | 2026-04-16 | rtl | `frame_rcv_ip` | `tb_int_longrun_sanity_test +TB_INT_LONGRUN_CASE_ID=1` | 41 | FEB long-run sanity, `require_lossless_feb`, stage `H0->H1` | Lane 1 dropped 3 terminating-tail hits because downstream closed before the last delayed frame arrived. | `aso_hit_type0_endofrun` pulsed as soon as `TERMINATING` saw local parser idle, even though the upstream 8b1k lane could still deliver a delayed final MuTRiG frame. | Add a guarded quiet window before asserting `hit_type0_endofrun`, and cover the delayed-tail terminate path in the direct and UVM IP benches. | ad61b7f | fixed |
| FRCV-2026-04-16-002 | 2026-04-16 | rtl | `frame_rcv_ip` | `tb_int_longrun_sanity_test +TB_INT_LONGRUN_CASE_ID=1` | 523 | FEB long-run sanity rerun after `ad61b7f`; terminate/drain closure to stage D | `RC_END` never completed, `H1` close markers stayed at zero, RBCAM `end_seen/drain_done` never asserted, and stage C/D ended with open frames. | The first fix waited for byte-lane quiet, but the revived MuTRiG contract keeps transmitting periodic empty frames during `TERMINATING`; those legal empty frames prevented the quiet guard from ever expiring, so `hit_type0_endofrun` never fired. | Close `TERMINATING` on the first empty frame after the final payload drain, keep the quiet window only as a fallback for benches without periodic empty frames, and cover the empty-frame fence in the direct and UVM IP benches. | bd47e3e | fixed |
| FRCV-2026-04-17-003 | 2026-04-17 | harness | `tb/uvm` | `all_buckets_frame_cfg_a`, `bucket_frame_basic_cfg_a`, `bucket_frame_edge_cfg_a`, `bucket_frame_error_cfg_a` | n/a | Continuous-frame signoff runs in `DV_REPORT.md` on the packaged `CFG_A` build; reproduced on 2026-04-17 and fixed/verified on 2026-04-18. | Before fix, the four CFG_A continuous-frame runs reported nonzero `unexpected_outputs` even with `UVM_ERROR=0`, `UVM_FATAL=0`, and clean counter checks; reproduced counts were `all_buckets_frame_cfg_a=2`, `bucket_frame_basic_cfg_a=2`, `bucket_frame_edge_cfg_a=3`, and `bucket_frame_error_cfg_a=26`. After fix, the 2026-04-18 reruns are clean on hard-fail metrics: `all_buckets_frame_cfg_a`=`22/32`, `738` txns, `unexpected_outputs=0`; `bucket_frame_basic_cfg_a`=`19/32`, `84` txns, `unexpected_outputs=0`; `bucket_frame_edge_cfg_a`=`13/32`, `24` txns, `unexpected_outputs=0`; `bucket_frame_error_cfg_a`=`25/32`, `76` txns, `counter_checks_passed=7`, `unexpected_outputs=0`. | Several documented control and malformed continuous-frame cases were under-modeled in `doc_case_engine_v2`, so the scoreboard could see real outputs against incomplete or incorrect expected-transaction bookkeeping. The red signoff state was a harness/spec-alignment bug, not a proven RTL data mismatch. | Fixed by adding explicit malformed/control case handlers in `frcv_cases.svh`, tightening CRC/output SVAs to meaningful valid/error cycles, rerunning the affected CFG_A signoff jobs, and refreshing `DV_REPORT.json` plus `REPORT/cross/` with the clean before/after evidence. Residual `FRCV_SCB_CASE_END` warning noise is tracked separately in `FRCV-2026-04-18-005`. | 8c88689 | fixed |
| FRCV-2026-04-17-004 | 2026-04-17 | harness | `tb/uvm` | `X034_long_bad_trailer_symbol_replaced_with_data`, `X035_long_extra_payload_after_declared_len`, `X036_long_new_header_inside_payload`, `X048_short_new_header_inside_payload`, `X049_short_extra_payload_after_declared_len` | n/a | Case-engine audit while checking whether malformed-packet injections can increment `crc_err_counter`; fixed and revalidated on 2026-04-18. | Several ERROR cases were reported as passing with `doc_case_engine_v2`, but the generic stimulus engine did not inject a distinct malformed byte pattern for those named scenarios, so the cases were not trustworthy evidence about malformed-packet behavior. | `run_documented_case_engine()` only recognized coarse name-derived knobs such as `bad_crc`, `valid_low`, `loss_sync`, `parity`, `decode`, `comma`, `missing`, `cut_mid`, and `stop_*`. It had no dedicated stimulus path for `bad_trailer`, `extra_payload_after_declared_len`, or `new_header_inside_payload`, so those case IDs aliased to cleaner traffic than their descriptions implied. | Fixed by adding explicit malformed-packet stimulus hooks for the affected case classes in `frcv_cases.svh` and rerunning the isolated ERROR cases. Verified passing on 2026-04-18 with distinct malformed injections for `X034`, `X035`, `X036`, `X048`, and `X049`. | fd47182 | fixed |
| FRCV-2026-04-18-005 | 2026-04-18 | harness | `tb/uvm` | `all_buckets_frame_cfg_a`, `bucket_frame_prof_cfg_a`, `P066_mode0_comma_every_64th_frame_counter`, `P069_mode0_comma_random_positions`, `P126_seed09_comma_mix_mode0_100k_cycles` | n/a | Continuous-frame queue-retirement cleanup from the CFG_A reruns first flagged on 2026-04-18; root-caused and reverified on 2026-04-21 under QuestaOne. | The earlier CFG_A reruns showed large pending queue depth and `FRCV_SCB_CASE_END` warnings because no-EOP mode-0 comma-abort transactions were left live across case boundaries. The 2026-04-21 canonical reruns are clean: `bucket_frame_prof_cfg_a`=`23/32`, `1157` txns, `unexpected_outputs=0`, `UVM_WARNING=0`; `all_buckets_frame_cfg_a_fix2`=`31/32`, `1462` txns, `counter_checks_passed=40`, `unexpected_outputs=0`, `UVM_WARNING=0`. | The generic documented-case engine treated any `comma` case as if it produced zero expected headers/hits and used `stop_boundary=none`, so mode-0 abort cases with real pre-abort headers or hits never retired cleanly in the scoreboard and leaked into the next case boundary. | Fixed by teaching `frcv_cases.svh` to model mode-0 comma-abort cases with the correct pre-abort header/hit counts, zero real EOPs, and `stop_boundary=\"mode0_abort\"`, then rerunning the isolated comma PROF repros plus the canonical `bucket_frame_prof_cfg_a` and `all_buckets_frame_cfg_a_fix2` signoff jobs. No new RTL bug was required to explain the failure. | - | fixed |
| FRCV-2026-04-18-006 | 2026-04-18 | rtl | `frame_rcv_ip` | `B027_csr_read_addr1_crc_counter` | n/a | BASIC bucket cleanup reruns after the continuous-frame control-case harness fixes; fixed and revalidated on 2026-04-18. | Before fix, a deliberately bad-CRC frame could drive `s_crc_result` away from the magic constant while both `p_crc_err_count` and CSR word1 remained zero. Reproduced isolated evidence was `csr=0`, `dbg_p_crc_err_count=0`, and `s_crc_result=0xec05` after `B027_csr_read_addr1_crc_counter`. After fix, the bad-CRC counter path is restored and the counter cases pass again. | The parser-level `K28.5` idle-BC abort path could preempt the dedicated `FS_CRC_CHECK` cycle just as the source returned to idle, so the bad-CRC compare retired without updating the registered CRC error counter. | Fixed by keeping the dedicated `FS_CRC_CHECK` cycle alive when `i_data=8'hBC` arrives from idle return, then rebaselining `B027`, `B060`, `B080`, `B113`, and `B114`. The `/data1` OSS formal bounded proof `oss_dut_crc_counter_prove` now also passes on the repaired RTL. | fd47182 | fixed |
| FRCV-2026-04-18-007 | 2026-04-18 | harness | `tb/uvm` | `B059_long_crc_bad_raises_error1_on_eop`, `B079_short_crc_bad_raises_error1_on_last_hit` | n/a | CRC sideband cleanup after closing `FRCV-2026-04-18-006` | The bad-CRC helper falsely reported that no external `crc_error` indication existed, even though the DUT emitted a one-cycle sideband pulse during frame retirement and the CRC counter incremented correctly. | `do_single_bad_crc_case()` started looking for `hit_error[1]` only after an extra 24-cycle delay, so it could easily miss the one-cycle pulse. The written case/SVA wording also assumed `error1` would ride on the final hit beat, which does not match the implemented between-process contract. | Fixed by starting the `hit_error[1]` watch during frame retirement, checking that the pulse stays off the `hit_valid/eop` beat, and tightening the active output SVA plus DV docs to the actual post-`FS_CRC_CHECK` sideband contract. Revalidated passing on 2026-04-18 with `B059` and `B079`. | fd47182 | fixed |
| FRCV-2026-04-21-008 | 2026-04-21 | harness | `tb/uvm` | `B039_long_zero_hit_frame_headerinfo_pulse`, `E004_terminating_command_same_cycle_open_frame_continues`, `E013_ctrl_valid_low_keeps_previous_state`, `E029_header_following_write_zero_then_one_requires_clocked_enable`, `E121_repeated_terminating_words_do_not_duplicate_eop`, `E123_header_after_error_state_needs_new_running_or_idle_monitoring`, `X099_csr_mask_low_then_illegal_ctrl`, `X119_cfgD_channel_width8_random_wide_values` | n/a | QuestaOne migration cleanup and isolated red-case reruns on 2026-04-21 | The edge/control cases timed out in isolated mode even though the DUT contract was still satisfiable, zero-hit `B039` over-checked frame counters that do not advance on header-only traffic, and cfg-specific isolated cases such as `X119` were accidentally compiled against the default `CFG_A` build unless the caller overrode the build tag manually. | The documented-case engine was still relying on coarse name-derived generic stimulus for several timing-sensitive control/CSR cases, and the isolated runner defaulted every case to `CFG_A`. That combination no longer matched the documented contract after the QuestaOne control-driver timing updates. | Fixed by auto-deriving `BUILD_TAG` from case IDs in `run_isolated_cases.py`, teaching `tb/uvm/Makefile` to map `CFG_B..CFG_G` to the packaged generics, relaxing the zero-hit strict counter check in `B039`, and adding explicit timing-aware handlers for `E004`, `E013`, `E029`, `E121`, `E123`, and `X099`. Revalidated green on 2026-04-21. | - | fixed |
| FRCV-2026-04-21-009 | 2026-04-21 | harness | `tb/uvm` | `P001_long_len0_10k_frames`, `P014_short_len0_10k_frames`, `P045_mask_low_long_idle_soak`, `P079_cfgA_default_long_soak`, `P118_seed01_clean_mix_100k_cycles`, `X119_cfgD_channel_width8_random_wide_values` | n/a | Practical isolated documented-case reruns after the QuestaOne migration on 2026-04-21 | The practical isolated sweeps and soak anchors were timing out with roughly half of the queued transactions still pending. Representative symptoms were `P001` stuck with `queue_depth=64` before the cap change and `X119` stuck with `queue_depth=32`; `P034`/`P045` additionally mis-scored quiescent blocked traffic as if frames should parse. | `doc_case_engine_v2` launched the next isolated practical frame immediately after the previous one, so the fresh header could arrive while the parser was still retiring the prior frame through the CRC/idle boundary. The same engine also lacked explicit blocked-soak handling for the quiescent `IDLE` and mask-low profilers. | Fixed by capping isolated practical `P*` repetitions to a small representative sample, inserting a one-cycle inter-frame settle for iterative isolated practical runs, and adding explicit blocked-frame handlers for `P034` and `P045`. After the 2026-04-21 reruns, the full previously red isolated set is green and no new RTL bug remained once the harness matched the documented contract. | - | fixed |
