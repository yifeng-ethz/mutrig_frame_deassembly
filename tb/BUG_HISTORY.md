# BUG_HISTORY

This ledger records real RTL and DV-harness bugs found during verification.
Each fixed bug must point at the testcase or regression that exposed it and the git commit that carried the fix.

| bug_id | date | scope | ip_or_tb | first_seen_case | first_seen_runtime_s | coverage_or_signoff_context | symptom | root_cause | fix_summary | fix_commit | status |
| --- | --- | --- | --- | --- | ---: | --- | --- | --- | --- | --- | --- |
| FRCV-2026-04-16-001 | 2026-04-16 | rtl | `frame_rcv_ip` | `tb_int_longrun_sanity_test +TB_INT_LONGRUN_CASE_ID=1` | 41 | FEB long-run sanity, `require_lossless_feb`, stage `H0->H1` | Lane 1 dropped 3 terminating-tail hits because downstream closed before the last delayed frame arrived. | `aso_hit_type0_endofrun` pulsed as soon as `TERMINATING` saw local parser idle, even though the upstream 8b1k lane could still deliver a delayed final MuTRiG frame. | Add a guarded quiet window before asserting `hit_type0_endofrun`, and cover the delayed-tail terminate path in the direct and UVM IP benches. | ad61b7f | fixed |
| FRCV-2026-04-16-002 | 2026-04-16 | rtl | `frame_rcv_ip` | `tb_int_longrun_sanity_test +TB_INT_LONGRUN_CASE_ID=1` | 523 | FEB long-run sanity rerun after `ad61b7f`; terminate/drain closure to stage D | `RC_END` never completed, `H1` close markers stayed at zero, RBCAM `end_seen/drain_done` never asserted, and stage C/D ended with open frames. | The first fix waited for byte-lane quiet, but the revived MuTRiG contract keeps transmitting periodic empty frames during `TERMINATING`; those legal empty frames prevented the quiet guard from ever expiring, so `hit_type0_endofrun` never fired. | Close `TERMINATING` on the first empty frame after the final payload drain, keep the quiet window only as a fallback for benches without periodic empty frames, and cover the empty-frame fence in the direct and UVM IP benches. | bd47e3e | fixed |
| FRCV-2026-04-17-003 | 2026-04-17 | harness | `tb/uvm` | `all_buckets_frame_cfg_a`, `bucket_frame_basic_cfg_a`, `bucket_frame_edge_cfg_a`, `bucket_frame_error_cfg_a` | n/a | Continuous-frame signoff runs in `DV_REPORT.md` on the packaged `CFG_A` build; reproduced on 2026-04-17 and fixed/verified on 2026-04-18. | Before fix, the four CFG_A continuous-frame runs reported nonzero `unexpected_outputs` even with `UVM_ERROR=0`, `UVM_FATAL=0`, and clean counter checks; reproduced counts were `all_buckets_frame_cfg_a=2`, `bucket_frame_basic_cfg_a=2`, `bucket_frame_edge_cfg_a=3`, and `bucket_frame_error_cfg_a=26`. After fix, the 2026-04-18 reruns are clean on hard-fail metrics: `all_buckets_frame_cfg_a`=`22/32`, `738` txns, `unexpected_outputs=0`; `bucket_frame_basic_cfg_a`=`19/32`, `84` txns, `unexpected_outputs=0`; `bucket_frame_edge_cfg_a`=`13/32`, `24` txns, `unexpected_outputs=0`; `bucket_frame_error_cfg_a`=`25/32`, `76` txns, `counter_checks_passed=7`, `unexpected_outputs=0`. | Several documented control and malformed continuous-frame cases were under-modeled in `doc_case_engine_v2`, so the scoreboard could see real outputs against incomplete or incorrect expected-transaction bookkeeping. The red signoff state was a harness/spec-alignment bug, not a proven RTL data mismatch. | Fixed by adding explicit malformed/control case handlers in `frcv_cases.svh`, tightening CRC/output SVAs to meaningful valid/error cycles, rerunning the affected CFG_A signoff jobs, and refreshing `DV_REPORT.json` plus `REPORT/cross/` with the clean before/after evidence. Residual `FRCV_SCB_CASE_END` warning noise is tracked separately in `FRCV-2026-04-18-005`. | pending | fixed |
| FRCV-2026-04-17-004 | 2026-04-17 | harness | `tb/uvm` | `X034_long_bad_trailer_symbol_replaced_with_data`, `X035_long_extra_payload_after_declared_len`, `X036_long_new_header_inside_payload`, `X048_short_new_header_inside_payload`, `X049_short_extra_payload_after_declared_len` | n/a | Case-engine audit while checking whether malformed-packet injections can increment `crc_err_counter` | Several ERROR cases are reported as passing with `doc_case_engine_v2`, but the generic stimulus engine does not inject a distinct malformed byte pattern for those named scenarios, so the cases do not provide trustworthy evidence about whether malformed packets drive the CRC counter. | `run_documented_case_engine()` only recognizes coarse name-derived knobs such as `bad_crc`, `valid_low`, `loss_sync`, `parity`, `decode`, `comma`, `missing`, `cut_mid`, and `stop_*`. It has no dedicated stimulus path for `bad_trailer`, `extra_payload_after_declared_len`, or `new_header_inside_payload`, so those case IDs alias to cleaner traffic than their descriptions imply. | Open. Add explicit malformed-packet stimulus hooks for the affected case classes, decide which should expect `crc_err_counter` increments versus silent drop/abort behavior, and refresh the ERROR bucket evidence after rerun. | - | open |
| FRCV-2026-04-18-005 | 2026-04-18 | harness | `tb/uvm` | `all_buckets_frame_cfg_a` | n/a | Post-fix CFG_A continuous-frame reruns after closing `FRCV-2026-04-17-003` | Latest reruns are green on hard-fail metrics, but they still emit many `FRCV_SCB_CASE_END` warnings and finish with nonzero pending queue depth: `all_buckets_frame_cfg_a` depth `716` / warnings `473`; `bucket_frame_basic_cfg_a` depth `31` / warnings `128`; `bucket_frame_edge_cfg_a` depth `88` / warnings `116`; `bucket_frame_error_cfg_a` depth `12` / warnings `85`. | `note_case_end()` warns whenever a case boundary arrives with a non-empty expected queue, but continuous-frame mode still does not reconcile per-case expectation lifetimes cleanly enough to distinguish harmless late matching from stale bookkeeping. | Open. Correlate per-case queue lifetimes in continuous-frame mode and decide whether to add settle, force-complete, or discard semantics, or to downgrade the warning once benign late matching is proven. | - | open |
